<?php

namespace Exercise;

use Model\Category;

class Ex4
{
    /**
     * Return an array with paths to all categories in $tree separated with $separator
     * @param Category $tree
     * @param string $separator
     * @return array
     */
    public static function path(Category $tree, $separator = '@')
    {
        return ['not implemented'];
    }
}
